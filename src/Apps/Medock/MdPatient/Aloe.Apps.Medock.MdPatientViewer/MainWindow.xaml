<Window x:Class="Aloe.Apps.Medock.MdPatientViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="患者閲覧" Height="500" Width="700">
    <ScrollViewer Margin="15" VerticalScrollBarVisibility="Auto">
        <StackPanel DataContext="{Binding PatientDetail}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10"
                       DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="患者コード" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PtCode}" Margin="0,5" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="患者名" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PtName}" Margin="0,5" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="カタカナ" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PtNameKatakana}" Margin="0,5" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="生年月日" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding BirthDate}" Margin="0,5" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="性別" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SexCode}" Margin="0,5" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="旧姓名" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding PtMaidenName}" Margin="0,5" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="別名" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding PtAliasName}" Margin="0,5" />

                <TextBlock Grid.Row="7" Grid.Column="0" Text="メモ" FontWeight="Bold" Margin="0,5" />
                <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding PtMemo}" Margin="0,5" TextWrapping="Wrap" />
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Window>
