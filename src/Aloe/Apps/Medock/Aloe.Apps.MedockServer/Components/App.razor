<!DOCTYPE html>
<html lang="ja" data-theme="light">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <title>Aloe Medock</title>
    <ResourcePreloader />
    
    <!-- Tailwind CSS Play CDN (must be loaded first) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- daisyUI v4 CDN (more stable than v5) -->
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.14/dist/full.min.css" rel="stylesheet" type="text/css" />
    
    <!-- D3.js for calculations -->
    <script src="https://cdn.jsdelivr.net/npm/d3@7/dist/d3.min.js"></script>
    <!-- Konva.js for Canvas rendering -->
    <script src="https://cdn.jsdelivr.net/npm/konva@9/konva.min.js"></script>
    
    <!-- Custom styles for container queries -->
    <style>
        /* コンテナクエリ対応 */
        .responsive-container {
            container-type: inline-size;
        }
        
        /* サイドバー切り替え用コンテナクエリ */
        @@container (min-width: 768px) {
            .sidebar-visible {
                display: flex !important;
            }
            .drawer-toggle-btn {
                display: none !important;
            }
        }
        
        @@container (max-width: 767px) {
            .sidebar-visible {
                display: none !important;
            }
            .drawer-toggle-btn {
                display: flex !important;
            }
        }
    </style>
    
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["Aloe.Apps.MedockServer.styles.css"]" />
    <ImportMap />
    <HeadOutlet />
</head>

@using Microsoft.AspNetCore.Components.Web

<body class="min-h-screen bg-base-200">
    <Routes @rendermode="InteractiveServer" />
    <ReconnectModal />
    <script src="@Assets["_framework/blazor.web.js"]"></script>
    <script src="js/calendar-canvas.js"></script>
</body>

</html>
