<Window x:Class="Aloe.Medock.Reservation.AloeMedockResvApp.Views.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Aloe.Medock.Reservation.AloeMedockResvApp.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
        Title="LoginWindow"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        Closing="LoginWindow_OnClosing">
    <DockPanel LastChildFill="True">
        <!-- ステータスバー -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="Processing..." />
            </StatusBarItem>
            <StatusBarItem Width="200">
                <ProgressBar x:Name="ProgressBar"
                             Height="20"
                             IsIndeterminate="True"
                             Margin="5,0,0,0" />
            </StatusBarItem>
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="1" HorizontalContentAlignment="Right">User</Label>
            <Label Grid.Column="0" Grid.Row="2" HorizontalContentAlignment="Right">Password</Label>

            <TextBox Grid.Column="1" Grid.Row="1" x:Name="UserTextBox"
                     Text="{Binding User.Value, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}"></TextBox>
            <TextBox Grid.Column="1" Grid.Row="2" x:Name="PasswordTextBox"
                     Text="{Binding Password.Value, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}"></TextBox>

            <Button Grid.Column="1" Grid.Row="3" x:Name="LoginButton" Content="Login" Click="LoginButton_OnClick" />
        </Grid>
    </DockPanel>
</Window>
