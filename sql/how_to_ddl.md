# HOW TO DDL

例えば `audit.a5er` のDDLを更新したい場合

`audit.a5er` を `A5:SQL Mk-2` で開く。

`メニュー &gt; ER図(D) &gt; DDLを作成する...(D) [Ctrl + D]` からDDLを出力する。

結果を `[repo]\sql\pg_setup\01_02_create_audit_tables_sjis.sql` に貼り付ける。
なお、cmd の psql コマンドを使用するため Shift_JIS で保存します。
