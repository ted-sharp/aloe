<Window
    x:Class="AloeSsmixSample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AloeSsmixSample"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    ResizeMode="CanResizeWithGrip"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox
            Grid.Column="0"
            AcceptsReturn="True"
            TextWrapping="WrapWithOverflow"
            VerticalScrollBarVisibility="Visible">
            退院サマリーなどの情報を表示します。
        </TextBox>

        <GridSplitter
            Grid.Column="1"
            Width="4"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch"
            Background="LightGray"
            ShowsPreview="True" />

        <Grid Grid.Column="2" Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,2">
                <TextBlock Width="72" DockPanel.Dock="Left">SS-MIX2 パス</TextBlock>
                <Button Click="OpenSsmixPathButton_OnClick" DockPanel.Dock="Right">...</Button>
                <TextBox x:Name="SsmixPathTextBox">C:\ssmix</TextBox>
            </DockPanel>

            <DockPanel Grid.Row="1" Margin="0,2">
                <TextBlock Width="72" DockPanel.Dock="Left">カルテ番号</TextBlock>
                <TextBox x:Name="KarteNumberTextBox">12345678901234567890</TextBox>
            </DockPanel>

            <StackPanel
                Grid.Row="2"
                Margin="2,0"
                Orientation="Horizontal">
                <TextBlock Width="68" />
                <Button
                    x:Name="VectorizeButton"
                    Margin="2"
                    Click="VectorizeButton_OnClick">
                    解析
                </Button>
                <Button
                    x:Name="MakeAdmissionSummary"
                    Margin="2"
                    Click="MakeAdmissionSummary_OnClick">
                    入院時サマリー
                </Button>
                <Button
                    x:Name="MakeDepartmentTransferSummary"
                    Margin="2"
                    Click="MakeDepartmentTransferSummary_OnClick">
                    転科サマリー
                </Button>
                <Button
                    x:Name="MakeWardTransferSummary"
                    Margin="2"
                    Click="MakeWardTransferSummary_OnClick">
                    転棟サマリー
                </Button>
                <Button
                    x:Name="MakeDischargeSummary"
                    Margin="2"
                    Click="MakeDischargeSummary_OnClick">
                    退院サマリー
                </Button>
            </StackPanel>

            <DockPanel
                Grid.Row="3"
                Margin="0,2"
                LastChildFill="True">
                <TextBox
                    x:Name="MessageTextBox"
                    DockPanel.Dock="Top"
                    IsReadOnly="True">
                    # システムメッセージ
                </TextBox>
                <TextBox
                    x:Name="LogTextBox"
                    AcceptsReturn="True"
                    TextWrapping="WrapWithOverflow"
                    VerticalScrollBarVisibility="Visible">
                    # ログ
                </TextBox>
            </DockPanel>
        </Grid>

    </Grid>
</Window>
